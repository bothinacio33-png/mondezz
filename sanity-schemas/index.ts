import { product } from "./product";

/**
 * Adicione no seu sanity.config.ts:
 *   import { schemaTypes } from "./schemas";
 *   ...
 *   schema: { types: schemaTypes }
 */
export const schemaTypes = [product];
